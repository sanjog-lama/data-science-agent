# =============================================================================
# DATA SCIENCE MULTI-AGENT SYSTEM - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and configure your settings
# cp .env.example .env

# =============================================================================
# MODEL CONFIGURATION (Choose ONE option)
# =============================================================================

# -----------------------------------------------------------------------------
# OPTION 1: DeepSeek (Recommended - Cost-effective & Powerful)
# -----------------------------------------------------------------------------
# Get API key from: https://platform.deepseek.com/
MODEL_TYPE=deepseek
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# -----------------------------------------------------------------------------
# OPTION 2: Self-Hosted LLM via vLLM (Default - Full control & Privacy)
# -----------------------------------------------------------------------------
# Requires running vLLM server locally or remotely
# Start server: vllm serve mistral-large:123b --host 0.0.0.0 --port 9000

# MODEL_TYPE=vllm
# VLLM_MODEL_NAME=mistral-large:123b
# VLLM_BASE_URL=http://localhost:9000/v1
# VLLM_API_KEY=EMPTY

# Alternative vLLM models (uncomment to use):
# VLLM_MODEL_NAME=mistral-7b              # Faster, less memory
# VLLM_MODEL_NAME=llama-3-70b             # Better quality
# VLLM_MODEL_NAME=qwen-72b                # Good for reasoning


# =============================================================================
# MCP SERVER CONFIGURATION (Required)
# =============================================================================

# -----------------------------------------------------------------------------
# Simple Configuration (No Authentication)
# -----------------------------------------------------------------------------
MCP_SERVERS_JSON=[{"url":"http://localhost:8080/mcp"},{"url":"http://localhost:8081/mcp"}]

# -----------------------------------------------------------------------------
# With Authentication (Bearer Token)
# -----------------------------------------------------------------------------
# MCP_SERVERS_JSON=[{"url":"http://localhost:8080/mcp","auth":"true"},{"url":"http://localhost:8081/mcp","auth":"false"}]
# MCP_AUTH_TOKEN=your-bearer-token-here
# MCP_AUTH_SCHEME=Bearer

# =============================================================================
# WEB INTERFACE & SESSION CONFIGURATION
# =============================================================================

# Enable Web Interface (Default: False - uses CLI only)
# Set to 'true' or '1' to enable web interface on 'uv run adk web'
SERVE_WEB_INTERFACE=false

# Session Service URI (Optional - for persistent sessions)
# If not provided, uses in-memory sessions (lost on restart)
# SESSION_SERVICE_URI=postgresql://user:password@localhost:5432/sessions
# SESSION_SERVICE_URI=redis://localhost:6379/0

# =============================================================================
# EXAMPLE CONFIGURATIONS
# =============================================================================

# -----------------------------------------------------------------------------
# Example 1: Quick Start with DeepSeek
# -----------------------------------------------------------------------------
# MODEL_TYPE=deepseek
# DEEPSEEK_API_KEY=sk-your-key-here
# MCP_SERVERS_JSON=[{"url":"http://localhost:8080/mcp"}]

# -----------------------------------------------------------------------------
# Example 2: Self-Hosted for Privacy
# -----------------------------------------------------------------------------
# MODEL_TYPE=vllm
# VLLM_MODEL_NAME=llama-3-70b
# VLLM_BASE_URL=http://localhost:9000/v1
# VLLM_API_KEY=EMPTY
# MCP_SERVERS_JSON=[{"url":"http://localhost:8080/mcp"}]

# -----------------------------------------------------------------------------
# Example 3: Production with Authentication
# -----------------------------------------------------------------------------
# MODEL_TYPE=deepseek
# DEEPSEEK_API_KEY=sk-prod-key
# MCP_SERVERS_JSON=[{"url":"https://mcp.company.com/mcp","auth":"true"}]
# MCP_AUTH_TOKEN=prod-bearer-token-xyz
# MCP_AUTH_SCHEME=Bearer


# =============================================================================
# NOTES
# =============================================================================
# 
# Model Selection Logic:
#   1. If DEEPSEEK_API_KEY is set → Uses DeepSeek (regardless of MODEL_TYPE)
#   2. If MODEL_TYPE=deepseek → Uses DeepSeek
#   3. Otherwise → Uses vLLM (default: mistral-large:123b at localhost:9000)
#
# MCP Server Notes:
#   - Set auth="true" for servers requiring authentication
#   - Provide MCP_AUTH_TOKEN and MCP_AUTH_SCHEME when using auth="true"
#
# Security:
#   - Never commit .env file to git
#   - Add .env to .gitignore
#
# =============================================================================